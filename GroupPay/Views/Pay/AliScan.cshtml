@{
    Layout = "_PayLayout";
}
<body class="Ali_body">
    <div class="Ali-header">
        <div class="logoimg"><img src="~/images/pay/ali.png" alt="" /></div>
        <div class="TJlogo Ali-color">
            <span> 充值系统</span>
        </div>
    </div>
    <div class="content" id="target"></div>
    <script id="ractiveTemplate" type="text/reactive">
        <div id="ImportantWarning" style="display:none;">
            <div class="table-cell">
                <div class="Warning">
                    <div class="Warning_title">重要提示</div>
                    <div class="Warning_item">一、此二维码只限本次使用</div>
                    <div class="Warning_item">二、并且在10分钟内充值有效</div>
                    <div class="Warning_item">三、超过时间支付、重复支付</div>
                    <div class="Warning_item">    一律不会上分，后果自负</div>
                    <div class="Warning_input">
                        <input class="cp" data-clipboard-text="{{amount}}" type="button" value="同意　复制金额" onclick='$("#ImportantWarning").hide();$("#mainContent").show();'/>
                    </div>
                </div>
            </div>
        </div>
        <div id="Loading">
            <table width="100%" height="100%" border="0">
                <tr>
                    <td align="center" valign="middle">
                        {{#if !error}}
                        订单号：<span id="pserno">{{mrn}}</span><br>
                        充值金额：<span class="notice-field" id="pprice">{{amount}}</span><br>
                        <img id="loadGif" src="/images/pay/loading.gif" />
                        {{/if}}
                        <div class="Loadingtxt">{{message}}</div>
                    </td>
                </tr>
            </table>
        </div>
        <div id="mainContent" style="display:none">
            <div class="Ali-details">
                <ul>
                    <li>订单号：{{mrn}}</li>
                    <li>
                        <div class="copytxt">
                            金　额：<red>{{amount}}</red>
                        </div>
                        <input type="button" value="复制金额" class="copybtn cp" data-clipboard-text="{{amount}}" />
                    </li>
                </ul>
            </div>
            <div class="Ali-qrcode">
                <div>{{accountName}}</div>
                <img src="{{qrCode}}" />
                <div style="display:none" id="error"></div>
                <div class="timer">
                    <span><font id="hour">00</font> 时</span>
                    <span><font id="min">00</font> 分</span>
                    <span><font id="sec">00</font> 秒</span>
                </div>
            </div>
            <div class="Ali-details">
                <ul>
                    <li>
                        <red>充值流程　　　充值金额与支付金额不一致不会上分</red><br />
                        <blue>
                            １、首先点击复制金额、截图保存到手机<br />
                            ２、用支付宝扫一扫、相册扫码、黏贴金额并支付<br />
                            ３、此二维码只能使用一次、重复支付不会上分
                        </blue>
                    </li>
                </ul>
            </div>
            <div class="Ali-details">
                <ul>
                    <li><red>重要提示：充值成功后５分钟未到帐，请马上与平台客服联系</red></li>
                </ul>
            </div>
        </div>
        <input type="button" value="返　回" class="TJ_back" onclick="window.location='@ViewBag.CallBackUrl'" />
    </script>
</body>
@section Scripts{
    <script type="text/javascript">
        var countDownSec = 600;
        $(document).ready(function () {
            initAliScanPage("@Html.Raw(@ViewBag.ErrorMsg)");
        });
    </script>
}
